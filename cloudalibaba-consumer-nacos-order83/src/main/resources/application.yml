server:
  port: 83

spring:
  application:
    name: nacos-order-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

#将要访问的微服务名称
service-url:
  nacos-user-service: http://nacos-payment-provider